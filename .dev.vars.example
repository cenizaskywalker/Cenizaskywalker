# Copy to .dev.vars and replace the placeholder with a newly generated webhook.
# Never commit .dev.vars or paste the production webhook into source code.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/replace-with-a-rotated-secret
